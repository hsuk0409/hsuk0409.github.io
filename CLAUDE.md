# 이 저장소에서 작업할 때

최현석의 이력서 사이트입니다. Jekyll + minimal-mistakes 기반이며,
`main`에 푸시하면 GitHub Actions가 자동 배포합니다.

## 먼저 읽을 것

디자인이나 페이지 콘텐츠를 수정하기 전에 **[DESIGN_GUIDE.md](DESIGN_GUIDE.md)를 반드시
읽으세요.** 색 규칙, 크기 위계, 컴포넌트 사용법, 마크다운 작성 규칙이 정리돼 있습니다.

## 자주 깨지는 지점

가이드에서 특히 놓치기 쉬운 것들입니다.

- **`--accent-pop`(`#14e0b8`)을 흰 배경 위 글자색으로 쓰지 마세요.** 대비가 1.4:1이라
  거의 안 보입니다. 글자에는 `--accent-color`(`#0d7a68`)를 씁니다.
- **`resume.md`에 회사를 추가할 때는 `<div class="job" markdown="1">`로 감싸세요.**
  래퍼가 없으면 좌측 타임라인 축과 점이 그려지지 않습니다.
- **`ai-experience.md`의 라벨 항목은 `<div class="kv" markdown="1">` 안에 넣고,
  콜론을 볼드 안쪽에 둡니다** (`- **라벨:** 내용`).
- **`.kv` 라벨 선택자의 `:first-child`를 지우지 마세요.** 값 안의 볼드까지 라벨로
  인식해 겹칩니다.
- **`.kv` 행을 `display: flex`로 바꾸지 마세요.** 값 안의 `<code>`가 조각납니다.
- **`portfolio-article.html`의 `{% include head/custom.html %}`을 지우지 마세요.**
  이력서·AI경험 페이지의 폰트가 전부 사라집니다.
- **`html { font-size: 16px }`을 바꾸면** 컨테이너 폭, 히어로 줄바꿈 기준점(800px),
  `.kv` 라벨 폭(122px)을 다시 재야 합니다. 전부 px 고정값입니다.

## 작업 방식

- 실제 사이트에 영향이 가므로 `main`에 직접 커밋하지 말고 브랜치에서 작업한 뒤
  PR로 올려주세요.
- 로컬 확인: `./serve.sh` → `http://127.0.0.1:4000`
- 수정 후에는 320 ~ 1440px 구간에서 가로 스크롤이 생기지 않는지 확인해주세요.

## 콘텐츠 원칙

이력서와 AI 활용 경험 페이지는 사실 기록입니다.

- 수치나 성과를 임의로 만들어 넣지 마세요.
- 기존 사실관계를 바꾸지 마세요.
- 마케팅 문구처럼 과장하지 마세요. 기술적 디테일은 유지합니다.
