# 디자인 가이드

이 사이트(홈 · 이력서 · AI 활용 경험)의 시각 규칙과 마크다운 작성 규칙을 정리한 문서입니다.
**디자인이나 콘텐츠를 수정하기 전에 이 문서를 먼저 읽어주세요.** 특히 `_pages/resume.md`,
`_pages/ai-experience.md`, `index.md`를 편집할 때는 아래 "마크다운 작성 규칙"을 지키지 않으면
스타일이 적용되지 않거나 레이아웃이 깨집니다.

---

## 1. 파일 구조

| 파일 | 역할 |
|---|---|
| `assets/css/portfolio-chrome.css` | **디자인의 중심.** 색·타이포 토큰, 헤더·푸터, 버튼, 이력서/AI경험 페이지의 모든 컴포넌트 |
| `_layouts/custom-home.html` | 홈 전용. 히어로와 프로젝트 카드 스타일이 `<style>` 안에 들어 있음 |
| `_layouts/portfolio-article.html` | 이력서·AI경험 공통 레이아웃 (본문 + 우측 목차) |
| `_sass/minimal-mistakes/skins/_custom-light.scss` | 블로그·포스트 등 minimal-mistakes 기본 페이지의 스킨. 위 토큰과 값을 맞춰둠 |
| `_data/projects.yml` | 홈의 프로젝트 카드 데이터 |

색을 바꿀 때는 `portfolio-chrome.css`의 `:root`와 `_custom-light.scss`를 **함께** 고쳐야
사이트 전체가 어긋나지 않습니다.

---

## 2. 색상

### 2단 액센트 — 가장 중요한 규칙

포인트 컬러가 두 개이고 **용도가 다릅니다. 서로 바꿔 쓰면 안 됩니다.**

| 변수 | 값 | 용도 | 흰 배경 대비 |
|---|---|---|---|
| `--accent-color` | `#0d7a68` | **텍스트·링크**. 글자에 쓰는 유일한 포인트 색 | 5.2:1 (WCAG AA 통과) |
| `--accent-pop` | `#14e0b8` | **채움·마커 전용**. 버튼 배경, 하이라이터, 불릿 점, 카드 상단 바 | 1.4:1 — 글자색으로 쓸 수 없음 |
| `--accent-ink` | `#0b2f28` | `--accent-pop` 위에 올리는 글자색 | 팝 민트 위 8.8:1 |
| `--accent-soft` | `rgba(20,224,184,.16)` | 아주 옅은 배경·밑줄 강조 |  |

> **하지 말 것** — `--accent-pop`을 흰 배경 위 글자색으로 쓰기. 명도 대비가 1.4:1이라 거의
> 보이지 않습니다. 민트를 글자에 쓰고 싶으면 `--accent-color`(딥 톤)를 쓰세요.

> **하지 말 것** — `--accent-pop` 배경 위에 흰 글자 올리기. 반드시 `--accent-ink`를 씁니다.

### 나머지 색

| 변수 | 값 | 용도 |
|---|---|---|
| `--bg-color` | `#fcfcfb` | 페이지 배경 (살짝 따뜻한 화이트) |
| `--text-main` | `#16181d` | 본문·제목 |
| `--text-muted` | `#5b6470` | 설명문, 보조 텍스트 |
| `--text-dark` | `#8b929c` | 라벨, 기간, 푸터 등 가장 낮은 위계 |
| `--border-color` | `#e2e4e7` | 모든 테두리·구분선 |
| `--card-bg-completed` | `#ffffff` | 활성 카드 배경 |
| `--card-bg-progress` | `#f5f6f5` | 비활성 카드, 참고 블록 배경 |

민트는 **절제해서** 씁니다. 현재 한 화면에 등장하는 지점은 대체로 세 곳 이하입니다
(이력서 보기 버튼 / 타임라인 점 / 불릿). 새 요소에 민트를 더할 때는 그 화면에서 이미 몇 번
쓰이고 있는지 먼저 보세요.

---

## 3. 타이포그래피

### 폰트

| 용도 | 폰트 |
|---|---|
| 제목, 로고, 버튼, 회사명 | `Outfit` |
| 본문 (한글 포함) | `Noto Sans KR` |
| 기간, 라벨, 태그, 코드 | `JetBrains Mono` |

세 폰트 모두 Google Fonts에서 불러옵니다. 로드 위치가 레이아웃마다 다릅니다.

- `custom-home.html` — `<head>`에서 직접 로드
- `portfolio-article.html` — `{% include head/custom.html %}`로 로드

> `portfolio-article.html`에서 `head/custom.html` include를 지우면 이력서·AI경험 페이지의
> 폰트가 전부 시스템 기본으로 떨어집니다. (실제로 있었던 버그입니다.)

### 크기 위계

루트가 `16px`입니다. 사이트의 거의 모든 치수가 `rem` 기반이라 **이 값 하나로 전체 크기가
정해집니다.**

| 요소 | 값 | 실제 크기 |
|---|---|---|
| 홈 히어로 h1 | `clamp(1.7rem, 5.2vw, 2.5rem)` | 27.2 ~ 40px |
| 홈 히어로 인사말 | `0.56em` (h1 기준) | 22.4px |
| 문서 제목 (이력서/AI 페이지 h1) | `2rem` | 32px |
| 섹션 제목 (h2) | `1.25rem` | 20px |
| 소제목 (h3) | `1.12rem` | 17.9px |
| 회사명 (`.job > h3 strong`) | `1.85em` | 21.3px |
| 본문 | `1rem` | 16px |
| 프로젝트 제목 | `0.94rem` | 15px |
| 상세 불릿 | `0.88rem` | 14.1px |
| 기간 (모노) | `0.72rem` | 11.5px |

위계 순서: **본문 16 < 소제목 17.9 < 섹션 제목 20 < 회사명 21.3 < 문서 제목 32**

> **루트 폰트 크기를 바꾸면** 아래 세 가지를 다시 재야 합니다. 전부 `px` 고정값이라 함께
> 움직이지 않습니다.
> 1. `.container`의 `max-width` — 히어로 본문의 쉼표 줄바꿈이 한 줄에 들어가는지
> 2. `custom-home.html`의 `@media (max-width: 800px)` 기준점 — 같은 이유
> 3. `.kv` 라벨 열의 `width: 122px` — 가장 긴 라벨이 한 줄에 들어가는지

### 한글 줄바꿈

본문에는 `word-break: keep-all`을 씁니다. 이게 없으면 "에이전-틱"처럼 단어 중간에서
끊깁니다. 새로 추가하는 텍스트 블록에도 넣어주세요.

---

## 4. 레이아웃

| 항목 | 값 | 비고 |
|---|---|---|
| `.container` | `max-width: 940px` | 홈. `padding: 80px 24px` |
| `.container--wide` | `max-width: 1100px` | 이력서·AI경험 |
| 히어로 폭 | `max-width: 900px` | |
| 목차 폭 | `260px` | 900px 이하에서 본문 아래로 내려감 |

> `.container`를 860px 이하로 줄이면 홈 히어로 본문의 쉼표 줄바꿈이 세 줄로 흐릅니다.
> 줄이려면 두 문단이 각각 2줄로 유지되는지 확인하세요.

### 반응형 분기점

| 폭 | 변화 |
|---|---|
| 900px | 목차가 본문 아래로 내려감 |
| 800px | 홈 히어로의 쉼표 줄바꿈 해제 (자동 줄바꿈으로) |
| 768px | 홈 프로젝트 카드 3열 → 1열 |
| 700px | AI 활용 원칙 카드 3열 → 1열 |
| 600px | `.kv` 카드가 라벨/값 세로 배치로 접힘 |

---

## 5. 컴포넌트

### 헤더

```html
<a href="/" class="site-brand">
  <span class="site-title">최현석</span>
  <span class="site-brand-sep"></span>
  <span class="site-role">Backend Engineer</span>
</a>
```

이름과 직군을 한 줄에 두고 세로 구분선으로 잇습니다. `align-items: center`로 상하 중앙
정렬합니다. `baseline`으로 바꾸면 작은 모노 라벨이 아래로 처집니다.

### 버튼

| 클래스 | 용도 |
|---|---|
| `.btn.btn--pop` | 팝 민트 채움. **"이력서 보기"에만** 씁니다 |
| `.btn.btn--ghost` | 흰 배경 + 테두리. 보조 동작 |
| `.btn--sm` | 헤더용 작은 크기 |

이력서가 이 사이트의 목적이라 그 링크만 버튼으로 승격하고 나머지는 텍스트 링크로 낮췄습니다.
**민트 버튼을 두 개 이상 늘리면 이 위계가 무너집니다.**

### 이력서 — 회사 타임라인 (`.job`)

좌측에 세로 축을 긋고 회사마다 민트 점을 찍습니다. 회사 사이 간격은 `margin`이 아니라
`padding-bottom`으로 줍니다. `margin`을 쓰면 축이 끊깁니다.

회사명은 `1.85em` + 굵기 700 + 민트 하이라이터로 강조합니다. 하이라이터는 글자 뒤에
깔리므로(`z-index: -1`) 명도 대비에 영향을 주지 않습니다.

### AI 경험 — 라벨/값 카드 (`.kv`)

라벨은 `position: absolute`로 왼쪽에 고정하고 값은 일반 블록으로 흐릅니다.

> **`display: flex`로 바꾸지 마세요.** 값 안의 `<code>` 같은 인라인 요소가 각각 별개의
> flex 아이템이 되어 문장이 조각납니다. (실제로 있었던 버그입니다.)

> **라벨 선택자의 `:first-child`를 지우지 마세요.** 값 안에 넣은 볼드까지 라벨로 인식해
> 겹쳐 버립니다. (이것도 실제로 있었던 버그입니다.)

라벨 열 폭은 `122px`이고 `white-space: nowrap`입니다. 더 긴 라벨을 쓰려면 `.kv--wide`
(176px)를 쓰거나 라벨을 줄이세요.

### AI 경험 — 참고 블록 (`.ref`)

"배경은 이력서 … 참고" 같은 부가 정보를 본문에서 빼내 좌측 민트 바가 붙은 블록으로 둡니다.
본문 문장이 괄호로 늘어지는 걸 막는 용도입니다.

### AI 경험 — 원칙 카드 (`.principles`)

3열 요약 카드입니다. **일부러 `h2`를 붙이지 않았습니다.** 본문 섹션이 아니라 보조 요약이라
목차에 잡히면 안 되기 때문입니다. 여기에 제목을 붙이면 목차에 항목이 하나 늘어납니다.

### 목차 (TOC)

홈의 프로젝트 카드와 같은 형태(흰 배경, 12px 라운드, 상단 민트 바)입니다. 현재 위치는
좌측 민트 바 + 옅은 민트 배경으로 표시합니다.

스크롤 연동은 minimal-mistakes의 Gumshoe가 처리합니다. 동작에 필요한 조건은 네 가지입니다.

1. `portfolio-article.html`에 `{% include scripts.html %}`
2. 레이아웃에 `<nav class="toc">` 마크업
3. `assets/js/main.min.js` 로드
4. CSS에 `.toc__menu .active a` 스타일

---

## 6. 마크다운 작성 규칙

**여기가 가장 자주 깨지는 부분입니다.** 아래 래퍼를 빠뜨리면 스타일이 전혀 적용되지 않습니다.

### 이력서에 회사를 추가할 때

```markdown
<div class="job" markdown="1">

### **회사명** (2026.01 - 현재)

_서버 개발자 / 정규직_

- **프로젝트 제목 (2026.01 - 진행중)**

  프로젝트 한 줄 설명.

  - **세부 항목**: 내용.

</div>
```

- `<div class="job" markdown="1">` 래퍼가 없으면 **타임라인 축과 점이 안 그려집니다.**
  CSS는 "다음 `h3` 전까지"를 선택할 수 없어서 감싸는 요소가 반드시 필요합니다.
- 회사명은 `**볼드**`, 기간은 그 뒤 괄호. 이 구조를 CSS가 그대로 활용해 분리합니다.
  볼드를 빼면 회사명 강조가 사라집니다.
- `markdown="1"` 속성이 없으면 안쪽 마크다운이 그대로 텍스트로 출력됩니다.

### Technical Skills 목록

```markdown
<div class="skills" markdown="1">

- **분류명:** 항목1, 항목2, 항목3

</div>
```

분류명이 한 줄, 항목 나열이 그 아래 줄로 내려갑니다. Education·Awards 목록은 이 래퍼를
쓰지 않습니다 (볼드 뒤에 기간이 붙는 구조라 줄바꿈하면 어색해집니다).

### AI 경험에 라벨/값 항목을 추가할 때

```markdown
<div class="kv" markdown="1">

- **라벨:** 내용 문장.
- **다른 라벨:** 내용에 **볼드**를 넣어도 됩니다.

</div>
```

- **콜론을 볼드 안쪽에** 둡니다 (`**라벨:**`). 이렇게 해야 CSS가 안 걸려도
  "라벨: 내용"으로 자연스럽게 읽힙니다.
- 값 안의 볼드는 **첫 번째가 아니어야** 합니다. 문장 맨 앞에 볼드를 또 넣으면 라벨로
  인식됩니다.
- 문장은 마침표로 끝냅니다. (이력서 불릿과 톤을 맞추기 위함)

### 참고 블록

```markdown
<div class="ref" markdown="1">
**배경** [이력서 — 항목 제목](/resume/#앵커)
</div>
```

### 홈 히어로 본문의 줄바꿈

```markdown
<p>앞 절 문장,<br class="wrap-br">뒤 절 문장입니다.</p>
```

- 쉼표 뒤에서 의미 단위로 끊습니다. `<br>` 앞의 **공백을 빼지 마세요.** 좁은 화면에서
  `<br>`이 숨겨질 때 공백까지 사라져 단어가 붙습니다.
- 800px 이하에서는 이 줄바꿈이 자동 해제됩니다.

---

## 7. 수정 후 확인할 것

1. **가로 스크롤** — 320 / 360 / 480 / 600 / 768 / 900 / 1440px에서 가로 스크롤이 없어야
   합니다. 특히 `white-space: nowrap`을 새로 쓴 경우 좁은 화면을 꼭 보세요.
2. **명도 대비** — 새 색을 글자에 썼다면 배경 대비 4.5:1 이상인지 확인합니다.
3. **줄바꿈** — 홈 히어로 두 문단이 1024px 이상에서 각각 2줄인지.
4. **목차 스크롤 연동** — 스크롤하면 현재 섹션이 민트로 표시되는지.
5. **로컬 미리보기** — `./serve.sh` → `http://127.0.0.1:4000`.
   CSS가 옛날 상태로 보이면 브라우저 강력 새로고침(⌘+Shift+R).

---

## 8. 이 디자인의 의도

한 줄로 요약하면 **"절제된 무채색 위에 민트 하나"** 입니다.

- 원래 팔레트는 GitHub Primer의 파랑(`#0969da`)을 거의 그대로 쓰고 있어서, 사이트가
  개인 브랜드보다 "깃허브 페이지"처럼 읽혔습니다. 그래서 고유한 민트로 바꿨습니다.
- 민트는 밝아서 글자색으로 쓸 수 없기 때문에, 딥 톤과 팝 톤을 나눈 2단 액센트가
  되었습니다. 이 구조를 무너뜨리면 가독성이나 대비 중 하나를 잃게 됩니다.
- 홈 · 이력서 · AI 경험 세 페이지가 같은 시각 문법을 씁니다. 섹션 라벨의 형태, 카드의
  테두리와 모서리, 민트를 쓰는 자리가 모두 같습니다. 새 요소를 만들 때는 먼저
  **기존 컴포넌트를 재사용할 수 있는지** 보세요.
- 이력서는 화면으로도 보지만 PDF로 저장되기도 합니다. 색이나 배경보다 **타이포와 여백으로**
  위계를 만드는 쪽을 택한 이유입니다.

---

## 9. 인쇄 / PDF 저장

`portfolio-chrome.css` 맨 끝의 `@media print` 규칙이 이력서(`/resume/`) 등
`portfolio-article` 레이아웃 페이지에서 Cmd/Ctrl+P를 눌렀을 때 방문자용 크롬
(헤더 네비·오른쪽 TOC·푸터)을 빼고 본문만 페이지에 맞게 출력되도록 합니다.

- **버튼을 두지 않는다.** 본인만 알고 쓰는 기능이라, "PDF로 저장" 같은 버튼이나
  안내문을 화면에 노출하지 않는 것이 의도입니다. 인쇄 다이얼로그를 여는 방법은
  브라우저 기본 단축키(Cmd/Ctrl+P)뿐입니다.
- **`resume.md`의 `print_append: /ai-experience/`** — 인쇄할 때만 AI 활용 경험
  페이지 내용을 이력서 아래에 이어 붙입니다(`portfolio-article.html` 레이아웃이
  `site.pages`에서 해당 URL을 찾아 `.content`를 가져와 `.print-append` 안에 렌더링,
  `.print-append`는 화면에서 항상 `display: none`). 원래 문서 제목이던 `<h1>`은
  `<h2>`로 낮춰 "Work Experience"·"Technical Skills"와 같은 급의 새 섹션으로
  붙입니다. `.portfolio-article > h3`처럼 직계 자식(`>`) 선택자는 래퍼 div 때문에
  안에서 안 먹으므로, 필요한 만큼만 `.print-append > h3`로 복제해 뒀습니다 — 원본
  규칙을 바꾸면 이 사본도 같이 바꿀 것. 다른 페이지를 추가로 붙이고 싶으면 같은
  front matter 키를 그 페이지에도 쓰면 됩니다(체인 방지를 위해 layout이 재귀적으로
  따라가진 않습니다).
- 실제 한 줄글 단위(중첩 `<ul>`이 없는 `<li>`, 중첩 `<ul>` 안의 `<li>`)만
  `break-inside: avoid`로 페이지 중간에서 안 잘리게 하고, `h2`/`h3`와 프로젝트
  제목 문단은 `break-after: avoid`로 제목만 페이지 끝에 남지 않게 합니다.
- 민트 하이라이터·타임라인 점처럼 배경색으로 표현되는 요소가 인쇄에서도 보이도록
  `body`에 `print-color-adjust: exact`를 강제합니다. (크롬에서 인쇄 시 "배경 그래픽"
  옵션이 꺼져 있어도 이 속성이 있으면 대체로 유지되지만, 안 보이면 옵션을 켜보라고
  안내하세요.)
- 새 컴포넌트를 추가할 때 배경색으로 의미를 전달한다면, 이 섹션에 인쇄 규칙을
  같이 추가할지 확인하세요.

**하지 말 것** — minimal-mistakes 테마의 기본 인쇄 스타일(컴파일된 `main.css`
안, 우리가 건드리지 않는 파일)이 `table, blockquote, pre, code, figure, li,
hr, ul, ol, a, tr { page-break-inside: avoid; }`를 사이트 전체에 무조건 걸어
둡니다. 이 사이트의 "프로젝트 하나"는 `<li>{제목+설명} + 중첩 <ul>{하위
불릿}>` 구조라, 이 테마 규칙을 그대로 두면 **프로젝트 하나 전체(하위 불릿
전부 포함)가 안 잘리는 한 덩어리**가 되어 페이지 남은 여백보다 조금만 커도
통째로 다음 페이지로 밀리고, 그만큼 앞 페이지에 큰 공백이 남는다. 실제로
겪은 버그이고, `.job`이나 `li` 전체에 `break-inside: avoid`를 다시 걸면
똑같이 재발한다. `.portfolio-article ul, ol`과 `li:has(> ul)`(프로젝트를
감싸는 li)은 반드시 `break-inside: auto`로 되돌려 두고, 최하위 `li`만
`avoid`를 걸 것.
